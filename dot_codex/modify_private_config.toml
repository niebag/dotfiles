#!/bin/sh

# Preserve unmanaged Codex state. Use tomlkit to retain TOML formatting; leave
# the file untouched if it is unavailable.

set -eu

# Read stdin before bootstrapping the venv.
INPUT="$(mktemp "${TMPDIR:-/tmp}/chezmoi-codex-config.XXXXXX")"
trap 'rm -f "$INPUT"' EXIT HUP INT TERM
cat > "$INPUT"

VENV="$HOME/.cache/dotfiles/venv"
PY="$VENV/bin/python3"

if [ ! -x "$PY" ]; then
  python3 -m venv "$VENV" </dev/null >/dev/null 2>&1 \
    && "$VENV/bin/pip" install -q tomlkit </dev/null >/dev/null 2>&1 \
    || true
fi

if [ ! -x "$PY" ] || ! "$PY" -c 'import tomlkit' >/dev/null 2>&1; then
  cat "$INPUT"
  exit 0
fi

"$PY" - "$INPUT" <<'PYEOF'
import sys
import tomlkit

with open(sys.argv[1]) as f:
    doc = tomlkit.parse(f.read())

doc["approval_policy"] = "never"
doc["sandbox_mode"] = "danger-full-access"

features = doc.get("features")
if features is None:
    features = tomlkit.table()
    doc["features"] = features
features["hooks"] = True
features["js_repl"] = False

sys.stdout.write(tomlkit.dumps(doc))
PYEOF
